<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.takeout.xianda.mapper.ProductMapper">
    <resultMap id="ProductPageVOMap" type="com.takeout.xianda.vo.ProductPageVO">
        <id column="id" property="id"/>
        <result column="product_name" property="productName"/>
        <result column="cover_image" property="coverImage"/>
        <result column="description" property="description"/>
        <result column="sale_price" property="salePrice"/>
        <result column="market_price" property="marketPrice"/>
        <result column="stock" property="stock"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="store_id" property="storeId"/>
        <result column="category_id" property="categoryId"/>

    </resultMap>

    <select id="selectProductPageJoinSku" resultMap="ProductPageVOMap">
        SELECT p.*,
        (SELECT s.sale_price FROM t_product_sku s WHERE s.product_id = p.id AND s.status = 1 ORDER BY s.id LIMIT 1) AS sale_price,
        (SELECT s.market_price FROM t_product_sku s WHERE s.product_id = p.id AND s.status = 1 ORDER BY s.id LIMIT 1) AS market_price,
        (SELECT SUM(s.stock) FROM t_product_sku s WHERE s.product_id = p.id AND s.status = 1) AS stock
        FROM t_product p
        WHERE p.status = 1
        <if test="query != null and query.productName != null and query.productName.trim() != ''">
            AND p.product_name LIKE CONCAT('%', #{query.productName}, '%')
        </if>
        ORDER BY p.create_time DESC
    </select>
</mapper>
